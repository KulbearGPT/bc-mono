# M7 内部 USD 钱包与全量审计门禁

日期：2026-07-21  
Story：M7-US-01 至 M7-US-07

| 要求 | 实现与可复核证据 | 结果 |
|---|---|---|
| USD-only | WalletAccount、WalletEntry、TopUp、ExternalRefundDebit、订单、礼物、指标与结算使用 USD minor units；迁移统一现有币种并加约束 | 通过 |
| 充值即时到账 | `createAdminTopUp` 在单事务追加 TopUp、CREDIT WalletEntry、钱包版本和审计 | 通过 |
| 充值阈值 | `amountMinor <= 500000` 允许 L1；`500001` 起要求 L2+，边界测试覆盖 | 通过 |
| 必填与可选凭证 | 客户、金额、渠道、交易号、paidAt、note 必填；JPEG/PNG/WebP/PDF 可选且私有 | 通过 |
| 渠道参考唯一 | 数据库唯一 `(payment_channel, external_transaction_id)`，幂等与冲突测试覆盖 | 通过 |
| 业务退款 | 仅向内部钱包追加 ORDER_REFUND_CREDIT | 通过 |
| 渠道退款扣款与非负 | 锁钱包、按 availableMinor 原子校验；不足整笔拒绝，数据库阻止负余额 | 通过 |
| 预留生命周期 | 订单/礼物提交创建，完成/批准捕获，取消/拒绝/过期释放，争议保持 | 通过 |
| 全量 mutation 审计 | 确定性清单覆盖 71 个 API 写路由和 10 个 Worker handler，成功/失败/拒绝均进入统一封套 | 通过 |
| AuditLogChange | 成功写逐对象 before/after 或摘要；失败/拒绝保留尝试头；敏感值脱敏 | 通过 |
| 原子回滚 | 成功业务事实与审计同事务，审计失败会回滚业务写入 | 通过 |
| 不可变 | 钱包、资金、凭证、审计头与明细 UPDATE/DELETE 由数据库拒绝 | 通过 |
| Provider 退役 | 支付 adapter、HTTP client、资金 Webhook、路由、环境变量和充值 URL 已删除；客户资金路径不读写第三方支付平台 | 通过 |
| Dashboard | 客户钱包面板支持充值、渠道退款扣款、流水、可选 receipt 上传与授权下载 | 通过 |
| Bot | 使用内部余额；余额不足仅提示联系客服并提交 receipt，不提供充值链接 | 通过 |
| 镜像一致 | outputs/docs OpenAPI、Prisma、验收和 TODO 镜像由合同测试逐字节验证 | 通过 |
| 文档与验收口径 | 主规格、OpenAPI、验收/fixtures、AGENTS、API 使用说明、交付包首页、业务配置与 HTML Backlog 均明确内部 USD 钱包为当前事实；历史 Provider Story 标为被 M7 覆盖 | 通过 |
| 验收 fixture 完整性 | acceptance-cases 中 162 个唯一 `FX-*` 均登记到 test-fixtures 且 JSON Pointer 可解析 | 通过 |
| 完整回归 | `npm test` 133 files / 682 tests；typecheck、build、Prisma validate、完整迁移、audit、diff check 全通过 | 通过 |

## 命令结果

```text
npm test                              133 files / 682 tests passed
npm run typecheck                     passed
npm run build                         passed
npm run db:validate                   Prisma schema valid
npm run db:verify:migration           migration-apply-ok; table_count=66
npm audit --audit-level=moderate      found 0 vulnerabilities
git diff --check                      passed
```

本门禁只证明 M7 代码、合同、迁移和自动化质量；真实 Discord/Dashboard UAT、恢复演练签署与最终发布批准仍按发布清单独立执行。
