# M13-US-04 Browser UAT

## 2026-08-05 环境与身份

- 本地 API：`http://localhost:3000/health` 返回 HTTP 200。
- 本地 Dashboard：`http://localhost:5173/admin/orders` 返回 HTTP 200。
- 员工亲自完成 Discord 登录；Dashboard 显示 `Sandbox 环境`、`L4 所有者` 与 `API ONLINE`。
- Codex 未读取、保存或代填员工账号凭证。
- 只读 fixture 审计：ACTIVE 员工仅 `L1_SUPPORT=1`、`L4_ADMIN_OWNER=1`；`L2_SUPERVISOR=1` 但为 DISABLED；不存在 L3。
- 产品只在当前业务 Guild 运营；不要求第二个真实 Guild，跨 Guild 隔离由自动化安全 fixture 证明。

## 七页真实浏览器结果

| 页面     | 排序组合 | 桌面 CARD/TABLE | 768px          | 375px          |
| -------- | -------: | --------------- | -------------- | -------------- |
| 订单     |        6 | 7/7 条详情一致  | 7 条语义表格   | 7 条行式列表   |
| 用户     |        6 | 4/4 条详情一致  | 4 条语义表格   | 4 条行式列表   |
| 陪玩     |        6 | 4/4 条详情一致  | 4 条语义表格   | 4 条行式列表   |
| 服务目录 |        8 | 3/3 条详情一致  | 3 条语义表格   | 3 条行式列表   |
| 服务套餐 |        8 | 3/3 条详情一致  | 3 条语义表格   | 3 条行式列表   |
| 礼物目录 |        8 | 1/1 条详情一致  | 1 条语义表格   | 1 条行式列表   |
| 礼物请求 |        8 | 空状态，无错误  | 空状态，无错误 | 空状态，无错误 |

- 共执行 50 个真实 API 排序组合，全部 URL 绑定正确且页面无 `alert`。
- 订单 `status=COMPLETED` 筛选得到 3 条；连续快速切换后最终为 `amountMinor asc`，结果金额为 `20.0, 20.0, 40.0` 猫条。
- 刷新后 `status=COMPLETED`、TABLE、金额升序和三条记录顺序全部恢复。
- CARD 与 TABLE 的首条均为 `P-3810D50D`，两种入口均成功加载相同订单详情。
- 浏览器 `press/keypress` 驱动没有移动焦点或触发原生控件，故不把真实键盘按键标记为通过；自动化已验证原生 button/select、accessible name、`tabIndex=0` 行与详情入口。
- 当前页面数据量均未超过单页上限，真实 UI 跨页不能执行；查询绑定游标与跨页唯一性由 M13-US-02/04 自动化覆盖。

## 结论与剩余项

当前结论：`PARTIAL_PASS`。L4 在当前业务 Guild 下的七页排序、双视图、响应式、空状态、筛选、URL 恢复、快速切换和详情 parity 已通过。真实键盘人工签署、L1/L2/L3 会话与产品/运营/QA 签署仍缺失；第二个真实 Guild 不属于发布条件，因此暂不声称 M13-US-04 发布完成。

## 2026-08-06 卡片间距复验

- 陪玩卡片视图工具栏 bottom 为 `705.046875px`，卡片网格 top 为 `717.046875px`，间距为 `12px`。
- 页面 viewport 与 `scrollWidth` 均为 `625px`，新增间距没有制造横向溢出。
- 同一相邻选择器覆盖订单卡片网格与其余六类业务卡片网格，表格视图不受影响。

## 2026-08-08 七页操作入口复验

- 旧布局在 1280×720 下把首张卡片动作放在 `765–873px`，导致详情、编辑、删除、审批或取消入口初始不可见。
- 新布局在七页首张卡片标题后固定展示有名称的“可用操作”区域；逐页 Chromium `toBeInViewport` 和动作可见断言为 `7/7 passed`。
- 切换 TABLE 后，同一记录的允许动作逐页复验可见；权限与状态条件继续由原 capability 和状态适用函数决定。
- 375×844 CARD 逐页复验同一动作可见，且 `document.documentElement.scrollWidth === 375`，没有新增横向裁切。
- 七页 CARD/TABLE 共 14 张 1280×720 截图保存在 `screenshots/action-visibility/`。
