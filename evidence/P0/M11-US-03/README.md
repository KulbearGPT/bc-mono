# M11-US-03 Discord 选秀面板、语音与客服通知

- 状态：本地候选；自动化门禁通过，真实 Guild UAT 与前置 `M9-US-18` 外部门禁尚未完成，因此 Story 保持 `IN_PROGRESS`。
- 验收：`AT-SEL-003`、`AT-SEL-006`；关联回归覆盖 `AT-DSP-017`～`AT-DSP-020`。
- RED：初始 `tests/m11-us-03-selection-discord.spec.ts` 因缺少 `@blackcat/bot/selection-discord` 导出失败；全量迁移后旧抢单/自动派单合同与测试共暴露 13 项失败。
- 实现：报名/撤回替代 first-wins 接单；Bot 提供 3、5、10、15、30 分钟等待预设，API 保留 1–30 整数分钟边界，客户可提前结束；九项目报名使用单个 String Select；候选名单私密分页；客户终选调用原子 API。
- Worker：`SELECTION_POOL_CLOSE` 自然截止可恢复；`SELECTION_POOL_SYNC` 幂等发布报名卡、创建或恢复 `user_limit=0` 私密选秀语音、通知客服、终选后撤销并移出未入选者；Discord 终态失败创建确定性唯一 `AUTOMATION_FAILURE` 客服任务。
- 恢复模拟：九名候选进入同一语音，重复执行 SELECTION 同步只创建一个语音房且复用带 nonce 的通知；终选三人后验证六名落选者逐一撤权、移出语音并收到包含三名公开昵称的本人结果。
- 退役：生产 Worker 不再消费 `DISPATCH_START/MESSAGE/TIMEOUT`；订单提交不再创建旧派单 Job；生产 Server 不注册旧 dispatch/accept/manual-dispatch 路由；Dashboard 不再提供客服派单；Bot/API 不再提供 availability 设置，Presence 只作诊断。
- 回归修复（2026-08-05）：真实订单 `P-336171B3` 证明提交事务仍写入旧 `ORDER_SUBMITTED_CHANNEL_SYNC`，Worker 在 0.57 秒后覆盖等待菜单。已停止这一过时 `PANEL_SYNC`，保留取消、就绪和后续生命周期同步，并原地恢复该 Discord 订单消息。
- 取消收敛修复（2026-08-07）：RED 证明待派单订单取消后活动候选池仍为 `COLLECTING`、报名仍为 `APPLIED`，且 Worker 拒绝 `CANCELLED` 同步。取消事务现会原子将活动池标记为 `CANCELLED/ORDER_CANCELLED`、追加池与报名失效事件、使所有有效报名 `INVALIDATED` 并写入稳定 `SELECTION_POOL_SYNC`；报名、撤回、关闭和终选 API 同时拒绝非 `PENDING_DISPATCH` 订单。Worker 原位关闭派单卡；若已进入选秀，则关闭客户候选消息并撤销、移出本轮报名者。RED 为 2 files / 3 failed；GREEN 目标 2 files / 12 tests、关联取消与候选池回归 10 files / 48 tests、类型检查、Prisma 校验及 000001–000029 空库迁移链通过。全仓 230 files / 1132 tests 中 1128 passed、4 个既有非关联门禁失败：缺少本地 `eslint` 可执行文件、M17 handler 路由/文件长度 2 项、已提交验收矩阵漂移 1 项；本修复涉及的目标与关联测试全部通过。关联验收：`AT-CAN-002`、`AT-SEL-005`、`AT-SEL-006`。
- 报名卡版本修复（2026-08-07）：RED 证明每次报名或撤回都会递增候选池 `row_version`，导致同一 Discord 报名卡只能供首位陪玩使用，客户随后用原卡关闭报名也会发生版本冲突。候选池版本现只表达 `COLLECTING → SELECTION → FINALIZED/CANCELLED` 生命周期迁移；报名与撤回只更新各自报名事实和实时人数，不再改写池版本。PostgreSQL 仍通过池行锁序列化报名、撤回和关闭，并在 API 内以 `now >= closesAt` 拒绝迟到操作，避免 Worker 延迟造成越界报名。RED 为 2 files / 5 failed / 8 passed；GREEN 目标 2 files / 13 tests，选秀及关联派单回归 20 files / 78 tests、类型检查与 `git diff --check` 通过。关联验收：`AT-SEL-002`、`AT-SEL-003`。
- 客户订单面板同步修复（2026-08-07）：RED 证明报名、撤回和报名关闭事务没有创建 `PANEL_SYNC`，且通用 Worker 不读取活动候选池，会把正确的报名菜单覆盖回旧“队伍正在集合”卡。三类事实变更现于同一事务写入稳定、可重试的面板同步任务；面板投影读取最新活动池及实时有效报名数，COLLECTING 展示轮次、人数、截止时间和提前结束按钮，零候选 SELECTION 恢复与首次一致的 1/3/5/10/15/30 分钟单下拉菜单，未开池的恢复面板也保留首次等待入口。RED 为 2 files / 3 failed / 13 passed；GREEN 目标 2 files / 16 tests，面板恢复、Worker、投影、选秀与派单关联回归 9 files / 56 tests、类型检查及 `git diff --check` 通过。旧 M5 PostgreSQL 恢复测试同步升级为完整迁移链验证。关联验收：`AT-SEL-003`、`AT-SEL-006`、`AT-DSP-017`～`AT-DSP-020`。
- 客户候选菜单终态修复（2026-08-07）：RED 证明终选完成后 `FINALIZED` Worker 只清理落选者权限并私信结果，客户原候选下拉仍可点击。Worker 现原位把该消息改为明确的入选名单摘要并清空组件；使用既有稳定 nonce 定位同一消息，重复执行不会新建候选卡。RED 1 file / 1 failed / 10 passed；GREEN 选秀 API、PostgreSQL 与 Discord Worker 3 files / 24 tests、类型检查及 `git diff --check` 通过。关联验收：`AT-SEL-003`、`AT-SEL-006`。
- 客服选拔通知终态修复（2026-08-07）：RED 证明选秀阶段发送到客服任务频道的开始通知，在订单取消或客户终选后仍停留在开始态。`CANCELLED` 与 `FINALIZED` Worker 现通过原稳定 nonce 原位更新同一客服消息：取消时明确关闭本轮，终选时展示入选名单，并清空旧组件；消息终态更新先于语音清理执行，因此缺少语音房不会阻断客服事实收敛。RED 1 file / 2 failed / 9 passed；GREEN 选秀及取消关联回归 5 files / 24 tests、类型检查及 `git diff --check` 通过。关联验收：`AT-SEL-006`、`AT-CAN-002`。
- 缺失派单卡恢复修复（2026-08-07）：真实订单 `P-0BBA84AA` 的第 4 轮已正确进入 `SELECTION` 且存在 1 名有效候选，但 `SELECTION_POOL_SYNC` 因原派单频道报名卡无法按稳定 nonce 找回而终止，导致客户候选菜单和客服选拔通知都没有发布。Worker 现在会在终态同步时原位更新既有报名卡；若卡片缺失，则用同一稳定 nonce 重建终态卡并继续创建语音、客户候选菜单与客服通知，不再让一个可恢复的 Discord 消息缺失截断整个后续流程。RED 1 file / 1 failed / 11 passed；GREEN 目标及关联回归 3 files / 16 tests、类型检查与 `git diff --check` 通过。对该真实订单执行幂等恢复后，Discord 订单频道出现“共 1 位候选”的 `bc:sp:s:` 选择组件；客户完成选择后数据库事实为订单 `ACCEPTED`（version 4）、池 `FINALIZED`（version 3）、1 名 `SELECTED`，证明候选界面和终选 API 已贯通。关联验收：`AT-SEL-003`、`AT-SEL-006`。
- 客户终选二次确认（2026-08-07）：RED 证明客户候选下拉会直接调用终选 API，缺少防误选确认步骤。下拉现在只生成仅当前用户可见的名单预览，不写入订单、候选池或参与人；预览以禁用选择框携带一至二十五名候选 ID，不依赖进程内临时状态，并提供“确认选择”和“返回重选”。只有确认按钮会沿原版本化路由调用 `finalizeOrderSelectionPool`，API 继续原子复核所有候选；返回按钮关闭预览，原候选菜单保持可用。RED 1 file / 1 failed / 12 passed；GREEN 目标 1 file / 13 tests。选秀事务、Discord Worker、Bot transport/feature/handler/error 关联回归 7 files / 49 passed，另 2 项为已知且与本次无关的 M17 旧路由解析和 707 行门禁失败；Piece discovery、全仓类型检查与 `git diff --check` 通过。真实 Discord Builder 序列化得到 2 个 Action Row、禁用候选 Select 和两个可达按钮，并从序列化组件无损还原两名 UUID。关联验收：`AT-SEL-004`、`AT-SEL-005`、`AT-DSP-014`、`AT-BOT-REV-004`。
- 派单频道收益文案精简（2026-08-07）：按产品确认，公开候选池报名卡及项目下拉仅展示项目名称和缺口人数，不再展示“默认预计收益”；收益事实与陪玩私密工作台不变。本次仅修改 Discord 展示文案，按要求不运行测试；`git diff --check` 通过。关联验收：`AT-SEL-002`、`AT-DSP-011`。
- 正式服务语音房切换（2026-08-07）：合同与运行时现在区分 `selection_voice_channel_id` 和正式 `voice_channel_id`。订单全部名额满足并进入 `ACCEPTED` 后，Worker 创建或恢复 `service-{订单号}` 私密房，初始只允许客户连接，先在订单 Ticket 提示并移动客户，再开放并移动入选陪玩；未入选者先撤权、移出并收到本人结果。部分入选仍保留 Selection 房继续下一轮，不提前创建 Service 房。旧 Selection 房改名为 `-closing` 并禁止重新连接，Bot 通过新增 `GuildVoiceStates`、ChannelUpdate 与 VoiceStateUpdate Listener 在最后一人退出后删除。客服“新订单协调”卡保留选秀协调入口并新增“进入服务房间”。RED 为 `@blackcat/bot/selection-channel-cleanup` 缺失（1 suite / 0 tests）；GREEN 为目标与合同/Worker 4 files / 98 tests、PostgreSQL/选秀/质量关联 6 files / 52 tests、287 条可复现验收矩阵、20 个 Sapphire Pieces、全仓 typecheck、Prisma validate、000001–000036 空库迁移链及 `git diff --check` 通过。全仓 `npm test` 完成 build 后仅余 3 个既有非关联失败：M17 的旧 refresh route 与 707 行门禁，以及本地缺少 `eslint` 导致的质量命令；真实 Guild 的多人移动顺序、私密权限与最后离房删除仍待 UAT。关联验收：`AT-DSP-020`、`AT-SEL-006`。
- 单候选返回与主动续轮修复（2026-08-07）：确认页“返回重选”不再只清空 ephemeral 消息，而是重新读取订单与候选池并渲染一张没有默认选中值的新候选卡，因此仅有一名候选时也可再次选择。所有非空候选卡新增“候选不合适，选择新一轮等待时间”的 1/3/5/10/15/30 分钟入口。统一业务 API 仅在客户显式携带被替换池 ID 与版本时允许续轮，并在同一事务把旧池 `FINALIZED`、仍有效报名 `NOT_SELECTED`、追加报名/池事件与旧池 `FINALIZED` Discord 同步，再创建新 `COLLECTING` 池；订单、参与人和原资金预留保持不变。RED 为 2 files / 2 failed / 26 passed；GREEN 目标与关联 5 files / 67 tests，扩大选秀/派单回归 11 files / 82 tests、PostgreSQL 空库事务、全仓 build/typecheck、157 条生产路由合同校验和 20 个 Sapphire Pieces 通过。`eslint` 与 npm script 的 Prettier 检查因本地缺少对应可执行文件未运行成功；已以最小 diff 和 `git diff --check` 复核。关联验收：`AT-SEL-005`、`AT-BOT-REV-004`。
- 返回重选权限回归修复（2026-08-07）：真实 request_id `req_15385bce-b71e-4cd0-8ff0-70f1893da462` 的审计证明同一次客服查看交互中，新增 `GET_ORDER/order.read` 因账号不是订单客户而失败，但 `LIST_ORDER_SELECTION_APPLICATIONS/order.selection_pool.read` 随后成功。返回重选现不再调用权限更严格且不必要的订单读取；新确认卡在返回按钮携带订单/池版本，已发出的旧确认卡则从同消息的确认按钮恢复版本，然后仅通过获授权候选接口重新渲染。RED 1 file / 2 failed / 14 passed；GREEN 目标 1 file / 16 tests、Bot 关联 6 files 中 5 files / 42 tests passed，余 2 项仍为既有 M17 旧 refresh route 与 707 行门禁失败；全仓 build/typecheck、20 个 Sapphire Pieces 与 `git diff --check` 通过。关联验收：`AT-SEL-005`、`AT-BOT-REV-004`。
- 返回重选 Components V2 渲染修复（2026-08-07）：真实 Discord interaction `1535503226504486952` 在把 Legacy Embed 确认页原位更新为 Components V2 候选页时被 Discord 以 `MESSAGE_CANNOT_USE_LEGACY_FIELDS_WITH_COMPONENTS_V2` 拒绝。公共更新渲染器现按 Discord 编辑合同显式把旧 `content` 重置为 `null`、`embeds` 重置为 `[]`，再发送 V2 `components` 与 `IS_COMPONENTS_V2` flag；Legacy Embed 更新保持原行为。RED 目标 1 file / 1 failed / 15 passed；GREEN 目标 1 file / 16 tests，渲染关联 5 files / 38 tests，全量 Bot 48 files 中 47 files / 273 tests passed，余 2 项仍为既有 M17 旧 refresh route 与 707 行门禁失败；Bot typecheck、全仓 build、20 个 Sapphire Pieces 与 `git diff --check` 通过。关联验收：`AT-SEL-005`、`AT-BOT-REV-004`。
- 重选轮次报名人数投影修复（2026-08-07）：真实订单 `P-A6FEB615` 的数据库事实确认第 2 轮有 1 名报名，报名、关闭与终选同步任务均成功；Discord 中停留在 0 人的是由候选页原位更新出的 ephemeral 交互副本，后续 Worker 只能持续更新订单主卡。新一轮创建事务现立即追加稳定 `PANEL_SYNC`，确保订单主卡从 0 人开始进入同步并由后续报名/撤回事实持续更新；ephemeral 副本改为不展示人数的“新一轮报名已开始”确认，并提供“查看实时订单卡”链接，避免把一次性响应误认为实时投影。RED 2 files / 2 failed / 18 passed；GREEN 目标 2 files / 20 tests，候选池、订单投影与 Worker 关联 6 files / 56 tests，API/Bot typecheck、全仓 build、20 个 Sapphire Pieces 与 `git diff --check` 通过；全量 Bot 为 47/48 files、274/276 tests，全仓为 226/230 files、1146/1151 tests，剩余 5 项均为既有非关联门禁：M17 两项、缺少本地 ESLint、验收矩阵漂移与既有 fixture 索引缺失。关联验收：`AT-SEL-003`、`AT-SEL-006`、`AT-DSP-017`。
- 续轮选秀语音映射修复（2026-08-08）：真实订单 `P-A6F6CA12` 的 `selection_voice_channel_id` 指向 Discord 已返回 404 的退役房间；下一轮 Worker 过去直接信任该数据库 ID，客服开始通知因而可能很快显示 `# unknown`。Worker 现在只复用 Guild 中实际存在、名称为活动态 `selection-{订单号}` 且位于正确分类的语音房；只存在 `-closing` 退役房或旧 ID 已删除时创建新房，并以投影旧 ID 为乐观并发前提替换订单映射（重复写入同一新 ID 仍幂等）。RED 为目标 1 file / 3 failed / 17 passed；GREEN 为目标 1 file / 20 tests，PostgreSQL 关联合计 2 files / 23 tests、全仓 typecheck 与 `git diff --check` 通过。扩大到 M11 合同/API/数据库/Discord 的 4 files / 37 tests 时为 35 passed、2 failed，失败均是提交前已存在的 `docs/`/`outputs/` 发布镜像漂移（频道清理主规格文案与 M9-US-19 交互映射），未改写这些非本修复文件。API lint 仍受本地未安装 `eslint` 可执行文件阻断，`npm run lint:api-dashboard` 与等价 `pnpm exec eslint` 均未运行测试代码。关联验收：`AT-SEL-003`、`AT-SEL-005`、`AT-DSP-017`、`AT-DSP-019`。
- 自动化证据：RED 分别复现旧 `ORDER_SUBMITTED_CHANNEL_SYNC` 和 30 个等待选项；GREEN 目标及关联回归 `4 files / 22 tests passed`，最新全量 `198 files / 974 tests passed`；`npm run typecheck` 与 `npm run db:validate` 通过。Discord GET 证据确认现有消息只有一个等待菜单，值为 `3,5,10,15,30`。
- 外部检查（2026-08-05）：配置的 API `/health` 与 `/ready` 均返回 200；同一 Bot Actor 请求既有订单路由得到 403，而候选池 applications 路由得到 404，证明该环境尚未部署 M11 路由。Railway CLI 返回 `Unauthorized` 且仓库未关联项目，因此未擅自部署，也不能把健康检查当作本 Story 的 Discord UAT。
- Discord 只读预检（2026-08-05）：Bot、Guild、频道、Role 与成员接口均返回 200；Guild 当前有 18 个频道、9 个 Role、6 名成员，Bot 的有效权限包含 Administrator。权限足以执行频道验收，但成员总数不足以构造合同要求的九名报名者加客户/客服场景，不能伪造 AT-SEL-003/006 签署。
- 剩余门禁：真实 Guild 九候选语音、客服通知、未入选权限/移出、Worker 重启与 Discord 故障重试；`M9-US-18` 的真实 Guild 面板恢复依赖尚未完成。
